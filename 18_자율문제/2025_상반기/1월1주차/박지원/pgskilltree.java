//프로그래머스 스킬트리 자바
//문자열, replaceAll, substring

import java.util.*;

class Solution {
    public int solution(String skill, String[] skill_trees) {
        int answer = 0;
        for (int i = 0; i < skill_trees.leng aceAll("[^" + skill + "]", "");
            String need = skill.substring(0, pre.length());
            if(pre.equals(need)) {
                answer++;
            }
        }
        return answer;
    }
}
